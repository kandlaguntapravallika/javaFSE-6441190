# GitDemoBranch

This project demonstrates **branching** and **merging** in Git.

## Steps Demonstrated
1. Create a new branch (`GitNewBranch`)
2. Add files and commit in the branch
3. Merge branch into `master`
4. Delete branch after merging

## Tools Used
- Git Bash
- P4Merge (for visual diff)
